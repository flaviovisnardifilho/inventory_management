extends layout

block content
  h1.title= title

  //- p Itens: #{item_list}

  ul.list
    each item in item_list 
      li
        a(href=item.url)  #{ item.name }

    else
      li There are no items.
